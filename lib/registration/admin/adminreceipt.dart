import 'package:flutter/material.dart';

class AdminReceiptPage extends StatelessWidget {
  const AdminReceiptPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
